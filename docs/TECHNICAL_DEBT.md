# Technical Debt

> Maintained by the `/backlog` skill. Items tagged 🤖 were proposed by automated
> review — review and edit them; they are suggestions, not facts. Promote good
> ones up into **Tracked debt** and delete the rest.
>
> Standards source: `CLAUDE.md` (no `unwrap()`/`expect()`/`panic!()` in library
> code; every public fn needs a doc test + unit test).

## Tracked debt

_Sourced from `TODO TD` / `TODO DEFECT` / `HACK` comments in the codebase._

- [ ] **Suit-weighted card sort** — change `Card` so sort is `Suit`-weighted first. (`src/cards.rs:514`)
- [ ] **Win-count refactor** — examine win count in case eval for refactoring opportunities. (`src/analysis/case_eval.rs:613`)
- [ ] **Add logging to BinaryCardMap** — no logging today. (`src/analysis/store/bcm/binary_card_map.rs:25`)
- [ ] **HUP width audit** — decide whether HUP should use `u64` vs `usize`. (`src/analysis/store/db/hup.rs:23`)
- [ ] **Masked matchups defect** — `TODO DEFECT` marker with no detail; needs triage. (`src/arrays/matchups/masked.rs:67`)

### Refactor backlog (`TODO RF`)

_11 `TODO RF` markers in source. The author flagged these as restructuring work; most are localized clean-ups, not behavior changes._

- [ ] **`arrays/two.rs` trait sorting** — sorting wanted for these traits "is starting to feel too complicated". (`src/arrays/two.rs:1513`, `:1555`)
- [ ] **`arrays/five.rs` hacks** — three stacked `RF`/"Hack" markers around hand evaluation. (`src/arrays/five.rs:252`, `:258`, `:267`)
- [ ] **`casino/state.rs` cleanup** — "This sucks" marker on state handling. (`src/casino/state.rs:119`)
- [ ] **`play/board.rs` clunky path** — `RF? Clunky`. (`src/play/board.rs:150`)
- [ ] **`flop_eval.rs` → trait** — extract into a trait. (`src/play/stages/flop_eval.rs:226`)
- [ ] **`case_eval.rs` case param** — change case parameter to `Two` to facilitate range calculations. (`src/analysis/case_eval.rs:99`)
- [ ] Misc `cards.rs` markers. (`src/cards.rs:80`, `:795`)

## 🤖 Automated review findings

_Generated by an automated code-review pass on first creation of this file
(2026-06-19). Each was checked against `CLAUDE.md`'s house rules. Verify before
acting — promote real ones into **Tracked debt**, delete false positives._

### House-rule violations (panics in library code)

- [ ] 🤖 **`select_all` raw `.unwrap()`** — `stmt.query(())` will panic if SQLite errors mid-iteration (lint suppressed). Suggested: propagate via `?`, return `Result<Vec<HUPResult>, rusqlite::Error>`. (`src/analysis/store/db/hup.rs:576`)
- [ ] 🤖 **`from_sorted_heads_up` assert** — `assert_eq!(first_ties, second_ties)` panics on asymmetric tie counters. Suggested: return `Result<Self, PKError>`. (`src/analysis/store/db/hup.rs:185`)
- [ ] 🤖 **`From<&SortedHeadsUp>` assert** — same assert in a `From` impl panics in production. Suggested: make it `TryFrom`. (`src/analysis/store/db/hup.rs:406`)
- [ ] 🤖 **`insert_many` is `todo!()`** — panics at runtime; trait is public API. Suggested: implement or return `PKError::NotImplemented`. (`src/analysis/store/db/hup.rs:488`)
- [ ] 🤖 **`NAMER` static `unwrap()`** — `LazyLock` init panics at first use if `RNG::new` fails. Suggested: fallback or `Result`-valued `LazyLock`; at minimum a `# Panics` doc. (`src/util/name.rs:6`)
- [ ] 🤖 **`receive_usize` `expect()`** — public fn panics on stdin read error. Suggested: return `Result<usize, std::io::Error>`. (`src/util/terminal.rs:140`)
- [ ] 🤖 **`Deck::get` unchecked index** — panics out-of-bounds with no `# Panics` doc. Suggested: document the panic and/or return `Option<Card>`. (`src/deck.rs:73`)
- [ ] 🤖 **`next_occupied_seat_after` silent default** — `u8::try_from(...).unwrap_or_default()` returns seat 0 on overflow, corrupting position logic for unusual table sizes. Suggested: propagate `PKError::InvalidSeatNumber`. (`src/casino/table.rs:907`)

### Missing doc tests on public APIs (house rule: every public fn needs one)

- [ ] 🤖 **`Deck` public methods** — `get`, `iter`, `to_par_iter`, `par_iter`, `array_iter`, `combinations`, `len`, `poker_cards`, `poker_cards_shuffled` lack doc tests. (`src/deck.rs`)
- [ ] 🤖 **`casino/table.rs` determiners** — `determine_game_phase`, `determine_betting_phase`, `determine_ceiling`, `determine_street_equity_possible`, `determine_street_equity`, `determine_hand_equity`, `commentary_last`, `commentary_last_player_action` lack doc tests. (`src/casino/table.rs`)
- [ ] 🤖 **`Board` constructors** — `Board::new`, `Board::turn_cards` lack doc tests. (`src/play/board.rs`)
- [ ] 🤖 **`hup.rs` query helpers** — `flip_mode`, `from_shift`, `matches`, `db_count`, `db_is_valid`, and siblings lack doc tests. (`src/analysis/store/db/hup.rs`)

> Overall health (per automated reviewer): reasonable structure and solid error
> types, but a recurring pattern of `assert_eq!`/`unwrap()`/`todo!()` in the
> HUP/SQL layer and widespread missing doc tests on `Deck`, `Board`, and table code.
